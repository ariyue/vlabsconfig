spring:
   profiles:
      active:
      - dev
---
spring:
  profiles: dev
  application:
      name: vlabsconfig-test-dev
---
spring:
 profiles: default
 application:
      name: vlabsconfig-test-default
